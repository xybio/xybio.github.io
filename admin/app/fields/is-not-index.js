import { t } from '../i18n/translater.js';

export const isNotIndex = {
  name: 'isIndex',
  label: t.fields.is_not_index,
  widget: 'hidden',
  default: false,
  required: false,
  i18n: 'duplicate'
};
