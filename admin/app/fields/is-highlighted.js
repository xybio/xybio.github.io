import { t } from '../i18n/translater.js';

export const is_highlighted = {
  name: 'is_highlighted',
  label: t.fields.is_highlighted,
  widget: 'boolean',
  default: false,
  required: false,
  i18n: 'duplicate'
};
