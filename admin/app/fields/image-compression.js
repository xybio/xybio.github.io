import { t } from '../i18n/translater.js';

export const image_compression = {
  name: 'linear',
  label: t.fields.image_compression,
  widget: 'boolean',
  default: false,
  required: false,
  i18n: 'duplicate'
};
