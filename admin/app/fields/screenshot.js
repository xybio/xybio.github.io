import { t } from '../i18n/translater.js';

export const screenshot = {
  name: 'screenshot',
  label: t.fields.screenshot,
  widget: 'boolean',
  default: false,
  required: false,
  i18n: 'duplicate'
};
