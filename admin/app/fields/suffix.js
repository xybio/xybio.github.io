import { t } from '../i18n/translater.js';

export const suffix = {
  name: 'suffix',
  label: t.fields.suffix.label,
  widget: 'string',
  required: false,
  i18n: 'duplicate',
  hint: t.fields.suffix.hint
};
