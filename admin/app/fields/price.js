import { t } from '../i18n/translater.js';

export const price = {
  name: 'price',
  label: t.fields.price.label,
  hint: t.fields.price.hint,
  widget: 'number',
  required: false,
  i18n: 'duplicate'
};
